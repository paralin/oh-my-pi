Context maintenance threshold reached. PENCILS DOWN.
Maintenance only: before more task work, {{#if create}}create{{else}}edit{{/if}} `{{displayPath}}` using the `{{toolName}}` tool.
{{#if create}}Create one bounded Org checkpoint with root metadata and exactly one active `* TODO` subtree.{{else}}Keep exactly one active `* TODO` subtree; update it in place. NEVER clear, recreate, rename, or replace the file.{{/if}}
Checkpoint MUST capture current objective; only immediately required skills; completed work; touched files; current proof; blockers/risks; executable next action; continuation source refs.
Remove completed-history subtrees and copied plans, queues, logs, traces, or large evidence; preserve them through links.
After successful `{{toolName}}`, NEVER reread or verify scratch. END TURN immediately: no task work, other tools, user-facing status, or path mention. Runtime observes the write; successor resumes.
