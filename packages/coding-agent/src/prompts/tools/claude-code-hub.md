Coordinate with OMP peers and task jobs through Hub.

Allowed operations are `list`, peer `send`, `inbox`, message or job `wait`,
`jobs`, and `cancel`. Process supervision and process-targeted `send` or `wait`
are unavailable in this runtime.
