[project]
name = "omp-ipython-skill-websearch"
version = "0.1.0"
description = "Web search skill for OMP's host-owned web service."
requires-python = ">=3.11,<3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/websearch"]
