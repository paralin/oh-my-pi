---
name: websearch
description: Search the web through OMP's host-owned web service.
type: python
python_import: websearch
python_callable: run
---

# Web Search

Search the web through OMP's typed host-owned web service.

```python
print(await websearch.run("latest OMP release"))
```
