[project]
name = "robomp-host-skill"
version = "0.1.0"
description = "Typed Python calls for Robomp task authority."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/robomp_host"]
